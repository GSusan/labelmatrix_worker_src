# labelmatrix_worker_src
codes of training and inference

Support training with task of segmentation, object detection, oriented object detection and pose. 

In terms of dataset conversion, only conversion for segmentation tasks is supported.【geojson_to_yolo.py】
